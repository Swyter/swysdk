#! Based on: http://stackoverflow.com/a/2440786
import decimal

def swytrailzro(num):
	# try:
		float(num)
		return ('%f' % num).rstrip('0').rstrip('.')
	
	# except ValueError:
		# for x in num:
			# x=('%f' % x).rstrip('0').rstrip('.')
	
		# return num